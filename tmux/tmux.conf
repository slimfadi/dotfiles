set -g prefix C-a
unbind C-b
bind C-a send-prefix

set -g base-index 1

bind r source-file ~/.tmux.conf \; display "relaoded!!"

bind v split-window -h
bind h split-window -v



set -g default-terminal "screen-256color"

set -g status-bg colour231
set -g status-fg colour52
